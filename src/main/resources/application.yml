spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dim1
    username: sa
    password: abc123DEF
#    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: com.example.oidtypeconversion.persistence.OnomaticPostgresDialect
#        format_sql: true
    hibernate:
      ddl-auto: create-drop
    #mapping-resources: "Jsonb.hbm.xml"

axon:
  axon server:
    enabled: false
  serializer:
    events: jackson
    general: jackson
    messages: jackson
server:
  port: 7777
logging:
  level:
    root: info
